% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dviGridSize.R
\name{dviGridSize}
\alias{dviGridSize}
\title{Size of joint assignment grid}
\usage{
dviGridSize(dvi = NULL, pairings = NULL, ignoreSex = FALSE, max = Inf)
}
\arguments{
\item{dvi}{A \code{dviData} object. If \code{pairings} is not supplied, candidate
pairings are taken from \code{dvi$pairings}, or generated with
\code{\link[=generatePairings]{generatePairings()}}.}

\item{pairings}{A list of character vectors, typically produced by
\code{\link[=generatePairings]{generatePairings()}}. If supplied, \code{dvi} is ignored.}

\item{ignoreSex}{A logical, passed on to \code{\link[=generatePairings]{generatePairings()}} if pairings are
generated from \code{dvi}.}

\item{max}{A positive number. Exact counting may stop once the count is known
to exceed this value. Default: \code{Inf}.}
}
\value{
A list with elements \code{size} (a number) and \code{type} (either
"exact" or "lower bound").
}
\description{
Calculates how many rows would be produced by \code{\link[=expand.grid.nodup]{expand.grid.nodup()}}, either exactly
or as a guaranteed lower bound. This is used to detect DVI problems where a joint
search would be prohibitive.
}
\details{
Exact counting is done without constructing the full table. The algorithm counts
matchings in the bipartite graph between victims and missing persons, treating the
non-assignment value "*" as a repeatable choice. For large problems, the function
returns a lower bound instead of the exact count. (See Value.)
}
\examples{
dviGridSize(example1)

pairings = generatePairings(example1)
dviGridSize(pairings = pairings)

}
\seealso{
\code{\link[=expand.grid.nodup]{expand.grid.nodup()}}, \code{\link[=dviJoint]{dviJoint()}}, \code{\link[=generatePairings]{generatePairings()}}, \code{\link[=ncomb]{ncomb()}}
}
